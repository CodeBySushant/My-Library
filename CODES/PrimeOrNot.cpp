// Not solved yet
#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"Enter a number: ";
    cin>>n;
    if(n>1){
        if(n%1==0 || n%n==0){
            cout<<"The number is a prime number.";
        }
        else if(n%i==0){
            cout<<"The number is not a prime number.";
        }
    }
    else{
        cout<<"The number is not a prime number.";
    }
    return 0;
}